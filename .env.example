# ============================================
# Factor 3: Configuration in env vars
# ============================================

PORT=3004
NODE_ENV=development

# Database (Factor 4)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=lostandfound
DB_USER=postgres
DB_PASSWORD=changeme

# Matching config
MATCH_THRESHOLD=70

# Logging (Factor 11)
LOG_LEVEL=info
